#include "stdafx.h"
#include "CppUnitTest.h"
//#include "../ImageSegmenter/Genetic_algorithm.hpp"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace GeneticAlgorithmTest
{		
	TEST_CLASS(UnitTest1)
	{
	public:
		
		TEST_METHOD(TestPixelSubtract)
		{
	/*		Pixel p1 = { 150, 160, 170 };
			Pixel p2 = { 160, 170, 190 };
			Pixel p_diff = p1 - p2;
			Pixel p_diff_true = { 10, 10, 20 };

			Assert::AreEqual(p_diff.r, p_diff.r);
			Assert::AreEqual(p_diff.g, p_diff.g);
			Assert::AreEqual(p_diff.b, p_diff.b);*/
		}

	};
}