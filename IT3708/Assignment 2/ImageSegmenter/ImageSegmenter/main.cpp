#include "image.hpp"
#include <iostream>

int main() {
	Image image("Test image.jpg");
	/*cv::Mat image;
	image = cv::imread("Test image.jpg", CV_LOAD_IMAGE_COLOR);

	cv::namedWindow("Display window", cv::WINDOW_AUTOSIZE);
	cv::imshow("Display window", image);

	cv::waitKey(0);
	*/
	//system("PAUSE");
	return 0;
}